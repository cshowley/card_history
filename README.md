# To generate historical price data for a card given its spec_id, run:

uv run card_price_action.py "<spec_id>"

# To run training script

uv run main.py --data-path training_data.csv

# 11/22

'''moved all existing work to v1/ directory, restarting from scratch in directory v2/'''
'''work in v1/ uses incomplete historical data, v2/ uses complete sales data starting in september 2025'''
